:root {
    {[ let brand = ds.currentBrand() /]}
    {[ for token in ds.tokensByType("Color", brand.id) ]}
        {[ let tokenName = readableVariableName(token, token.groupPath(), "") /]}
        --{{ tokenName }}: {[ inject "rendered-color" context token /]};
    {[/]}
}
